module.exports = {name: "play", aliases: ["p", "tocar"], run: async (client, message, args) => {let channel = message.member.voice.channel;let myVoice = message.guild.me.voice.channel;if (!channel) return message.reply({ content: `⚠ **|** Você precisa estar conectado a um canal de voz.` });if (myVoice && channel.id !== myVoice.id) return message.reply({ content: `🎵 **|** Eu já estou a tocar em outro canal de voz!` });const musica = args.join(" ");if (!musica) return message.reply({ content: `<:JB_pesquisa:886981227935060038> **|** Digite algo para eu pesquisar!` });message.reply({ content: `<:JB_pesquisa:886981227935060038> **|** Pesquisando por: \`${musica}\`` });var a = setTimeout(() => { client.distube.play(message, musica);clearInterval(a); }, 1000);}}